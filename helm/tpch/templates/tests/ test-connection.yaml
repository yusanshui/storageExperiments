apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tpch.fullname" . }}-test-connection"
  labels:
    {{- include "tpch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tpch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never